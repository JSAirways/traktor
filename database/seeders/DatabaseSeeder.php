<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * The v2 rewrite reuses production data, so automated seeding is intentionally
     * left blank. Follow the onboarding steps in docs/SCHEMA_NOTES.md to prepare
     * any required fixtures (e.g., admin user, demo content) when testing locally.
     */
    public function run(): void
    {
        //
    }
}
